# Repository settings managed by Probot Settings
# https://github.com/probot/settings

repository:
  name: okcolor
  description: Zero-config, build-time color modernizer for Vite and Tailwind CSS. Converts legacy Hex, RGB, HSL, HWB, and named colors to perceptually uniform OKLCH.
  homepage: https://tonyblu331.github.io/okcolor
  topics:
    - oklch
    - color
    - css
    - vite
    - tailwindcss
    - build-tools
    - typescript
    - build-time
  has_issues: true
  has_projects: false
  has_wiki: false
  has_downloads: true
  default_branch: main
  allow_squash_merge: true
  allow_merge_commit: false
  allow_rebase_merge: true
  delete_branch_on_merge: true

branches:
  - name: main
    protection:
      required_pull_request_reviews:
        required_approving_review_count: 1
        dismiss_stale_reviews: true
        require_code_owner_reviews: false
      required_status_checks:
        strict: true
        contexts:
          - TypeScript Tests
          - Build Package
      enforce_admins: false
      required_linear_history: true
      restrictions: null
