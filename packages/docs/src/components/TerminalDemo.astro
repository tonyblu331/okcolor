<div class="terminal-wrapper">
  <div class="terminal">
    <div class="terminal-header">
      <span class="terminal-dot red"></span>
      <span class="terminal-dot yellow"></span>
      <span class="terminal-dot green"></span>
      <span class="terminal-title">okcolor</span>
    </div>
    <div class="terminal-body">
      <div class="line" data-step="0">
        <span class="prompt">$</span>
        <span class="cmd">okcolor audit src/styles.css</span>
      </div>
      <div class="line output" data-step="1">
        <span>Scanning src/styles.css ...</span>
      </div>
      <div class="line output highlight" data-step="2">
        <span>Found 47 legacy colors:</span>
      </div>
      <div class="line output indent" data-step="3">
        <span>hex:     23</span>
      </div>
      <div class="line output indent" data-step="4">
        <span>rgb():   12</span>
      </div>
      <div class="line output indent" data-step="5">
        <span>hsl():   8</span>
      </div>
      <div class="line output indent" data-step="6">
        <span>named:   4</span>
      </div>
      <div class="line" data-step="7">
        <span class="prompt">$</span>
        <span class="cmd">okcolor src/styles.css -o dist/styles.css</span>
      </div>
      <div class="line output" data-step="8">
        <span>Transformed 47 colors in 0.83 ms</span>
      </div>
      <div class="line" data-step="9">
        <span class="prompt">$</span>
        <span class="cmd">okcolor check --max-legacy-colors=5</span>
      </div>
      <div class="line output success" data-step="10">
        <span>✓ 3 legacy colors found (max: 5) — passed</span>
      </div>
      <div class="line" data-step="11">
        <span class="prompt">$</span>
      </div>
    </div>
  </div>
</div>

<style>
  .terminal-wrapper {
    margin: 1.5rem 0;
    border-radius: 12px;
    overflow: hidden;
    box-shadow:
      0 4px 24px rgba(0, 0, 0, 0.12),
      0 1px 4px rgba(0, 0, 0, 0.08);
  }

  .terminal {
    background: #0d1117;
    font-family: 'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace;
    font-size: 13px;
    line-height: 1.6;
  }

  .terminal-header {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 10px 14px;
    background: #161b22;
    border-bottom: 1px solid #21262d;
  }

  .terminal-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .terminal-dot.red { background: #ff5f56; }
  .terminal-dot.yellow { background: #ffbd2e; }
  .terminal-dot.green { background: #27c93f; }
  .terminal-title {
    margin-left: auto;
    font-size: 11px;
    color: #8b949e;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .terminal-body {
    padding: 14px;
    overflow-x: auto;
  }

  .terminal-body .line {
    opacity: 0;
    white-space: nowrap;
  }

  .terminal-body .prompt {
    color: #56d364;
    margin-right: 8px;
    user-select: none;
  }

  .terminal-body .cmd {
    color: #e6edf3;
  }

  .terminal-body .output {
    color: #8b949e;
    padding-left: 1.2em;
  }

  .terminal-body .output.highlight {
    color: #f0883e;
    font-weight: 600;
  }

  .terminal-body .output.indent {
    padding-left: 2.4em;
    color: #7d8590;
  }

  .terminal-body .output.success {
    color: #3fb950;
  }

  /* Typing cursor */
  .terminal-body .line:last-child .prompt::after {
    content: '';
    display: inline-block;
    width: 2px;
    height: 1em;
    background: #56d364;
    margin-left: 4px;
    vertical-align: text-bottom;
  }

  @keyframes cursor-blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
  }

  .terminal-body .line:last-child .prompt::after {
    animation: cursor-blink 1.2s step-end infinite;
  }

  /* Staggered reveal */
  .terminal-body .line {
    animation: line-fade-in 0.35s ease-out forwards;
  }

  .terminal-body .line:nth-child(1) { animation-delay: 0.3s; }
  .terminal-body .line:nth-child(2) { animation-delay: 0.7s; }
  .terminal-body .line:nth-child(3) { animation-delay: 1.1s; }
  .terminal-body .line:nth-child(4) { animation-delay: 1.5s; }
  .terminal-body .line:nth-child(5) { animation-delay: 1.9s; }
  .terminal-body .line:nth-child(6) { animation-delay: 2.3s; }
  .terminal-body .line:nth-child(7) { animation-delay: 2.7s; }
  .terminal-body .line:nth-child(8) { animation-delay: 3.5s; }
  .terminal-body .line:nth-child(9) { animation-delay: 4.2s; }
  .terminal-body .line:nth-child(10) { animation-delay: 5.0s; }
  .terminal-body .line:nth-child(11) { animation-delay: 5.8s; }
  .terminal-body .line:nth-child(12) { animation-delay: 6.6s; }

  @keyframes line-fade-in {
    from {
      opacity: 0;
      transform: translateY(4px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .terminal-body .line {
      opacity: 1;
      animation: none;
    }
  }
</style>
